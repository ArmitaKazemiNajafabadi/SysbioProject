module coupling
export directionallyCoupled

function fullyCoupled(S,rev, solver)
    #=
    finds all fully coupled reaction sets

    solver: determines if a system of equation is solvable
    =#
end


function directionallyCoupled(S, rev, i, solver)
    
end

end